# Web app
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.0
python-dotenv==1.0.1

# Data / retrieval
pandas==2.2.2
numpy==2.0.2
pyarrow==22.0.0            # parquet engine (read data/index meta) — required at runtime

# Embeddings + LLM client. Versions pinned to a known-good set: FlagEmbedding
# 1.2.10 predates transformers 5.x (which breaks its `from transformers.trainer
# import *`), so the ML cluster is pinned to the versions that built the index.
FlagEmbedding==1.2.10
transformers==4.57.3
tokenizers==0.22.1
sentence-transformers==5.1.2
huggingface_hub==0.36.0
accelerate==1.12.0
datasets==4.4.1
safetensors==0.7.0
scipy==1.16.3
scikit-learn==1.7.2
